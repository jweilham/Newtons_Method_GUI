from numpy import linspace

# Modularized to avoid multiple declarations
# x = every number from -10 -> 10  (for graphing functions, graphs function for x values of -10 -> 10)
# linspace (start, stop, samples)
# Generate 200 samples to get a smooth line for user's aesthetic pleasure
x_range = linspace(-10, 10, 100)
